fastapi>=0.110
uvicorn>=0.27
pydantic>=2
numpy>=1.24
scikit-learn>=1.3
openai>=2.0
stripe>=9

# Optional, recommended for PDF ingestion.
PyMuPDF>=1.24

# Optional, local semantic embeddings if you already have a local model.
sentence-transformers>=2.7

# Optional, only if you want local model generation through transformers.
transformers>=4.40
torch>=2.2
